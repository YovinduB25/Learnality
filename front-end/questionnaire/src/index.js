import React from 'react';
import ReactDOM from 'react-dom';
import './quizL.css';
import App from './quizL';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
